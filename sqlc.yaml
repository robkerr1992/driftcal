version: "2"
sql:
  - engine: "sqlite"
    queries: "db/queries/"
    schema: "db/migrations/"
    gen:
      go:
        package: "sqlcdb"
        out: "gen/sqlcdb"
        emit_json_tags: true
        emit_empty_slices: true
