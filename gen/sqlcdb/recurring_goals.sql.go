// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: recurring_goals.sql

package sqlcdb

import (
	"context"
	"database/sql"
)

const createGoal = `-- name: CreateGoal :one
INSERT INTO recurring_goals (
    label, category, duration_minutes, times_per_week,
    preferred_time_of_day, energy_level, earliest_hour, latest_hour,
    allowed_days, min_gap_between_hours, priority
) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
RETURNING id, label, category, duration_minutes, times_per_week, preferred_time_of_day, energy_level, earliest_hour, latest_hour, allowed_days, min_gap_between_hours, is_active, priority, created_at, updated_at
`

type CreateGoalParams struct {
	Label              string         `json:"label"`
	Category           string         `json:"category"`
	DurationMinutes    int64          `json:"duration_minutes"`
	TimesPerWeek       int64          `json:"times_per_week"`
	PreferredTimeOfDay sql.NullString `json:"preferred_time_of_day"`
	EnergyLevel        string         `json:"energy_level"`
	EarliestHour       string         `json:"earliest_hour"`
	LatestHour         string         `json:"latest_hour"`
	AllowedDays        sql.NullString `json:"allowed_days"`
	MinGapBetweenHours int64          `json:"min_gap_between_hours"`
	Priority           int64          `json:"priority"`
}

func (q *Queries) CreateGoal(ctx context.Context, arg CreateGoalParams) (RecurringGoal, error) {
	row := q.db.QueryRowContext(ctx, createGoal,
		arg.Label,
		arg.Category,
		arg.DurationMinutes,
		arg.TimesPerWeek,
		arg.PreferredTimeOfDay,
		arg.EnergyLevel,
		arg.EarliestHour,
		arg.LatestHour,
		arg.AllowedDays,
		arg.MinGapBetweenHours,
		arg.Priority,
	)
	var i RecurringGoal
	err := row.Scan(
		&i.ID,
		&i.Label,
		&i.Category,
		&i.DurationMinutes,
		&i.TimesPerWeek,
		&i.PreferredTimeOfDay,
		&i.EnergyLevel,
		&i.EarliestHour,
		&i.LatestHour,
		&i.AllowedDays,
		&i.MinGapBetweenHours,
		&i.IsActive,
		&i.Priority,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deactivateGoal = `-- name: DeactivateGoal :exec
UPDATE recurring_goals SET
    is_active = 0,
    updated_at = CURRENT_TIMESTAMP
WHERE id = ?
`

func (q *Queries) DeactivateGoal(ctx context.Context, id int64) error {
	_, err := q.db.ExecContext(ctx, deactivateGoal, id)
	return err
}

const getGoal = `-- name: GetGoal :one
SELECT id, label, category, duration_minutes, times_per_week, preferred_time_of_day, energy_level, earliest_hour, latest_hour, allowed_days, min_gap_between_hours, is_active, priority, created_at, updated_at FROM recurring_goals WHERE id = ?
`

func (q *Queries) GetGoal(ctx context.Context, id int64) (RecurringGoal, error) {
	row := q.db.QueryRowContext(ctx, getGoal, id)
	var i RecurringGoal
	err := row.Scan(
		&i.ID,
		&i.Label,
		&i.Category,
		&i.DurationMinutes,
		&i.TimesPerWeek,
		&i.PreferredTimeOfDay,
		&i.EnergyLevel,
		&i.EarliestHour,
		&i.LatestHour,
		&i.AllowedDays,
		&i.MinGapBetweenHours,
		&i.IsActive,
		&i.Priority,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listActiveGoals = `-- name: ListActiveGoals :many
SELECT id, label, category, duration_minutes, times_per_week, preferred_time_of_day, energy_level, earliest_hour, latest_hour, allowed_days, min_gap_between_hours, is_active, priority, created_at, updated_at FROM recurring_goals
WHERE is_active = 1
ORDER BY priority DESC, id
`

func (q *Queries) ListActiveGoals(ctx context.Context) ([]RecurringGoal, error) {
	rows, err := q.db.QueryContext(ctx, listActiveGoals)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []RecurringGoal{}
	for rows.Next() {
		var i RecurringGoal
		if err := rows.Scan(
			&i.ID,
			&i.Label,
			&i.Category,
			&i.DurationMinutes,
			&i.TimesPerWeek,
			&i.PreferredTimeOfDay,
			&i.EnergyLevel,
			&i.EarliestHour,
			&i.LatestHour,
			&i.AllowedDays,
			&i.MinGapBetweenHours,
			&i.IsActive,
			&i.Priority,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateGoal = `-- name: UpdateGoal :one
UPDATE recurring_goals SET
    label = ?,
    category = ?,
    duration_minutes = ?,
    times_per_week = ?,
    preferred_time_of_day = ?,
    energy_level = ?,
    earliest_hour = ?,
    latest_hour = ?,
    allowed_days = ?,
    min_gap_between_hours = ?,
    priority = ?,
    updated_at = CURRENT_TIMESTAMP
WHERE id = ?
RETURNING id, label, category, duration_minutes, times_per_week, preferred_time_of_day, energy_level, earliest_hour, latest_hour, allowed_days, min_gap_between_hours, is_active, priority, created_at, updated_at
`

type UpdateGoalParams struct {
	Label              string         `json:"label"`
	Category           string         `json:"category"`
	DurationMinutes    int64          `json:"duration_minutes"`
	TimesPerWeek       int64          `json:"times_per_week"`
	PreferredTimeOfDay sql.NullString `json:"preferred_time_of_day"`
	EnergyLevel        string         `json:"energy_level"`
	EarliestHour       string         `json:"earliest_hour"`
	LatestHour         string         `json:"latest_hour"`
	AllowedDays        sql.NullString `json:"allowed_days"`
	MinGapBetweenHours int64          `json:"min_gap_between_hours"`
	Priority           int64          `json:"priority"`
	ID                 int64          `json:"id"`
}

func (q *Queries) UpdateGoal(ctx context.Context, arg UpdateGoalParams) (RecurringGoal, error) {
	row := q.db.QueryRowContext(ctx, updateGoal,
		arg.Label,
		arg.Category,
		arg.DurationMinutes,
		arg.TimesPerWeek,
		arg.PreferredTimeOfDay,
		arg.EnergyLevel,
		arg.EarliestHour,
		arg.LatestHour,
		arg.AllowedDays,
		arg.MinGapBetweenHours,
		arg.Priority,
		arg.ID,
	)
	var i RecurringGoal
	err := row.Scan(
		&i.ID,
		&i.Label,
		&i.Category,
		&i.DurationMinutes,
		&i.TimesPerWeek,
		&i.PreferredTimeOfDay,
		&i.EnergyLevel,
		&i.EarliestHour,
		&i.LatestHour,
		&i.AllowedDays,
		&i.MinGapBetweenHours,
		&i.IsActive,
		&i.Priority,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
